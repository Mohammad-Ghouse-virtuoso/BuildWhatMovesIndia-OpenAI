import { RouteShell } from "@/components/shared/route-shell";

export default function ReviewPage() {
  return (
    <RouteShell
      eyebrow="Step 5 · Review"
      title="Check the authority, request, and mock fee"
      description="Filing in this prototype is simulated. Registration numbers will use the DEMO/ prefix."
    />
  );
}
