import { RouteShell } from "@/components/shared/route-shell";

export default function InformationPage() {
  return (
    <RouteShell
      eyebrow="Step 3 · Records"
      title="These are the records that could answer you"
      description="A controlled list of record categories will be selectable here. Shell only in Phase 0."
    />
  );
}
