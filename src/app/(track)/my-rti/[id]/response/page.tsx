import { RouteShell } from "@/components/shared/route-shell";

export default function RtiResponsePage() {
  return (
    <RouteShell
      eyebrow="Response"
      title="What the records show"
      description="Synthetic documents, a grounded summary, and unanswered items will appear here in Phase 2–3."
    />
  );
}
