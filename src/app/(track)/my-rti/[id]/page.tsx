import { RouteShell } from "@/components/shared/route-shell";

export default function RtiDetailPage() {
  return (
    <RouteShell
      eyebrow="Timeline"
      title="What happened to this request"
      description="Status comes from software, not the model. Shell only in Phase 0."
    />
  );
}
