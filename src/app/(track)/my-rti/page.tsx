import { RouteShell } from "@/components/shared/route-shell";

export default function MyRtiPage() {
  return (
    <RouteShell
      eyebrow="My RTIs"
      title="Track synthetic requests"
      description="Phase 1 seed will list demo applications. Continue as the demo citizen — there is no real login."
    />
  );
}
