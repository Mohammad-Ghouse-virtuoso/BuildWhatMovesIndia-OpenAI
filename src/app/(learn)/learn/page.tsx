import { RouteShell } from "@/components/shared/route-shell";

export default function LearnPage() {
  return (
    <RouteShell
      eyebrow="Learn"
      title="RTI, without the portal maze"
      description="Right to Information lets you request existing records from a public authority. This prototype covers Central public authorities only — not State RTI portals. We help you phrase a records request. We do not give legal advice, and we do not file with the government."
    />
  );
}
